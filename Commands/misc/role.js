const Discord = require("discord.js")

module.exports={
	name: "joingame",
	description: "you can add a role with this",
	catgory: "misc",
	run:async(bot,message,args)=>{
		if(!member.roles.cache.some(role => role.name === '@everyone') return message.channel.send("You do not have the role needed")
			
		if(aMember.roles.has(728078042500431952)) {
			return channel.message.send(`${aMember} you already have this role.)
		} else {
			await aMember.addRole('728078042500431952')
			
		}
	}
}
